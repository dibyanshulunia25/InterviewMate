{
  "name": "interview-mate-root",
  "version": "1.0.0",
  "description": "Root configuration for single web service deployment",
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend && npm install --prefix backend",
    "start": "npm start --prefix backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
